{
  "name": "pi-command-history",
  "version": "0.2.0-local",
  "description": "Folder-based persistent command history for pi with ctrl+r fuzzy search.",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/datspike/pi-command-history.git"
  },
  "author": "Ross Z",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "command-history",
    "folder-history",
    "persistent-history",
    "fuzzy-search"
  ],
  "files": [
    "extensions",
    "package.json",
    "README.md"
  ],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.75.3",
    "@earendil-works/pi-tui": "^0.75.3",
    "@xterm/headless": "^6.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "pi": {
    "extensions": [
      "extensions/index.ts"
    ]
  }
}
